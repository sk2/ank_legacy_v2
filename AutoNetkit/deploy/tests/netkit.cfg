[Netkit Hosts]
    [[trc1]]
    # dev in adelaide
    host = trc1
    username = autonetkit
    active = 1
    collect data = 1
    #collect data commands = 'zebra: show ip route', 'bgpd: show ip bgp summary', 'ssh: uname -a'
    collect data commands = 'ssh: uname -a'
